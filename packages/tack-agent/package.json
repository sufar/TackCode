{
  "name": "@tackcode/tack-agent",
  "version": "0.1.0",
  "private": true,
  "description": "ZCode Protocol bridge that lets the ZCode desktop/web frontend drive a pi-rs agent (pi-rs --mode rpc).",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "tack-agent": "./bin/tack-agent.mjs"
  },
  "scripts": {
    "start": "node bin/tack-agent.mjs",
    "smoke": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
